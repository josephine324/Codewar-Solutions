/* u have an array and you want to compare it to the function*/
function all( arr, fun ){
    if (arr.length===0) return true;
    let fun = greater_than_9;
  for (fun of arr){
    return true;
  }
}
console.log(all([2,3,1,4],greater_than_9));
