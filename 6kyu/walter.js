function fizzBuzzFactory (arr){
    return function(){
      return 
    }
  }
  // you have an array paired [(3, "Fizz"), (5, "Buzz"), (15, "FizzBuzz")]
  //we need to create  a function so that when it is given n it returns the string corresponding to it 
  //now forexample the number 16 now the string that will be output will correspond the biggest number that divides it.
